# Sanitizer Patterns

Patterns regex Python supplémentaires (en plus des patterns de base).
Ajoute ici ton nom réel, hostname, toute info personnelle à scrubber.
Un pattern par ligne, préfixé par `- `.

- `DISCORD_TOKEN\s*[=:]\s*\S+`
- `sk-ant-[A-Za-z0-9\-_]+`
- `ghp_[A-Za-z0-9]+`
